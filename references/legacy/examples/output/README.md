# Generated Example Outputs

This directory is for generated outputs from `examples/*.py`.

The current convention is one subdirectory per example:

```text
examples/output/<example_name>/
```

Generated files are ignored by git. Keep this README tracked so the output
root exists and documents the convention.
